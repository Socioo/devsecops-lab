{
  "name": "devsecops-app",
  "version": "1.0.0",
  "description": "DevSecOps CI/CD Lab Application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo 'No tests configured yet' && exit 0",
    "lint": "eslint app.js || true",
    "audit": "npm audit --production --audit-level=moderate || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "nodemon": "^3.0.2",
    "supertest": "^7.2.2"
  }
}
